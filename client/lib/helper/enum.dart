enum AuthStatus {
  NOT_DETERMINED,
  NOT_LOGGED_IN,
  LOGGED_IN,
}

enum SortUser {
  VERIFIED,
  ALPHABETICALY,
  NEWEST,
  OLDEST,
  MAX_FOLLOWER,
}

enum NotificationType {
  Follow,
}
